`ic_pos.py` contains the `checkout` function and the `Checkout` class. 

Tests are in `ic_pos_tests.py` and can be run from this directory with `python ic_pos_tests.py -v` (`-v` optional for more verbose output.) 
